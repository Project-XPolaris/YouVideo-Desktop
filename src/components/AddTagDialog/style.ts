import { createStyles, makeStyles } from '@mui/styles'

const useStyles = makeStyles(() =>
  createStyles({
    root: {
    },
    content: {

    },
    input: {

    }
  })
)

export default useStyles
